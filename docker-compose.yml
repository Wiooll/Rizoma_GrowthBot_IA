services:
  rizoma:
    build: .
    container_name: rizoma-app
    ports:
      - "8000:8000"
    environment:
      RIZOMA_OPEN_BROWSER: "0"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
