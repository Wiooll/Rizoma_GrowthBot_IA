{
  "name": "rizoma-mobile-pwa",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test --experimental-test-isolation=none tests/*.test.mjs && python -m unittest discover -s tests",
    "spellcheck": "node scripts/check-spelling.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
